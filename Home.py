import streamlit as st

st.set_page_config(
    page_title="Hello",
    page_icon="👋",
)

st.write("# Welcome to GadaGeni! 👋")

st.markdown(
    """
   gadageni is an 3 on 1 streamlit app that could do , usual cchat with ai trough OpenAI's GPT model and claude.
   it also include website summarize. also my personal pick applicant filter (limited cv's). this is just fun project
"""
)